name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  observatory:
    name: Reproducible Observatory validation
    uses: ./.github/workflows/observatory-ci.yml
    permissions:
      contents: read
