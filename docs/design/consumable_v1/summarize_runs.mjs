import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import assert from 'node:assert/strict';
const root=path.dirname(fileURLToPath(import.meta.url));
const detail=JSON.parse(fs.readFileSync(path.resolve(root,'../../../artifacts/dev/consumable-v1/runs.json'),'utf8'));
assert.equal(detail.length,640,'The campaign is incomplete');
const selected={assassin:'execution',gardien:'bastion',arpenteur:'sniper',thaumaturge:'pyre'};
const names={v1:'V1 retenue, sous-ensemble apparié',greedy:'Une action anticipée',aggressive:'Dépense facile, une action anticipée',conserve:'Très économe, une action anticipée',no_rare:'Aucune carte rare ou supérieure',no_shops:'Aucun achat ni troc au marchand',no_gear:'Aucun équipement trouvé ou acheté',no_relics:'Aucune relique trouvée ou garantie',deck15:'Préparation maximale 15',deck30:'Préparation maximale 30',fallback:'Aucune carte jouée',old_drop_variance:'Anciennes tailles et probabilités des sacs normaux'};
let text='# Variantes de run comparées\n\nToutes les cases ci-dessous utilisent les **mêmes dix graines 10001–10010** et la première spécialisation de chaque classe. La référence est le sous-ensemble correspondant des 25 graines principales, pas leur moyenne complète. Une case compte des victoires sur dix, avec une incertitude importante.\n\n| Variante | Assassin | Gardien | Arpenteur | Thaumaturge |\n|---|---:|---:|---:|---:|\n';
for(const scenario of Object.keys(names)){const values=Object.entries(selected).map(([cls,spec])=>{const group=detail.filter(r=>r.scenario===scenario&&r.class===cls&&r.spec===spec&&r.seed<10011);assert.equal(group.length,10);return group.filter(r=>r.completed).length+'/10';});text+='| '+[names[scenario],...values].join(' | ')+' |\n';}
text+='\nLes variantes de politique modifient à la fois la valeur accordée à la sécurité/aux copies et leur anticipation ; elles ne sont pas des profils de compétence humaine. Les autres variantes gardent le contrôleur de référence. Les achats et l’équipement peuvent modifier le stock, la pioche et la trajectoire : les différences ne sont pas toutes monotones. Aucune conclusion fine n’est fondée sur un écart d’une victoire sur dix.\n\nSans équipement, le budget épargné reste disponible pour les achats de cartes et soins. Sans marchand, les refuges gratuits restent actifs. Sans relique, l’Obole et les autres effets disparaissent mais les taux de cartes restent identiques. La variante sans rare retire seulement les cartes rares/légendaires/dieu/immortelles, pas les élites.\n';
const example=detail.find(r=>r.scenario==='v1'&&r.class==='gardien'&&r.spec==='bastion'&&r.completed);assert.ok(example);
text+=`\n## Exemple observé : Gardien/Bastion, graine ${example.seed}\n\nChaque ligne provient du modèle, pas d’une partie humaine. « Stock après récompenses » inclut la réserve et les éventuels achats/trocs ; il ne faut pas le confondre avec la taille du deck engagé.\n\n| Combat | Niveau | Tours | Copies jouées | Secours offensifs | PV à la victoire | Cartes engagées | Stock après récompenses | Or après services |\n|---|---:|---:|---:|---:|---:|---:|---:|---:|\n`;
for(const t of example.timeline)text+=`| ${t.fight} | ${t.level} | ${t.turns} | ${t.cards} | ${t.basic} | ${(t.hpRatio*100).toFixed(1)} % | ${t.stockBefore} | ${t.stockAfterRewards??t.stockAfter} | ${t.goldAfter??t.gold} |\n`;
text+=`\nBilan des copies : 15 initiales + ${example.dropped} trouvées + ${example.bought} achetées, avec les trocs et ventes de la politique, donnent ${example.stock} restantes après ${example.consumed} consommées. Dépenses : ${example.spending.cards} or de sacs, ${example.spending.gear} d’équipement, ${example.spending.heal} de soins. Or final : ${example.gold}. Le contrôle de conservation dans le modèle inclut séparément chaque entrée et sortie ; cette ligne d’exemple ne déduit pas le troc à partir d’un solde approximatif.\n\nLes détails des 640 trajectoires se régénèrent dans \`artifacts/dev/consumable-v1/runs.json\`. Les synthèses et intervalles par groupe sont dans [run_summary.csv](run_summary.csv).\n`;
fs.writeFileSync(path.join(root,'COMPARAISONS_RUN.md'),text);console.log('Matched comparisons and one complete run exported.');
