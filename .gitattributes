# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Achilles canonical character-only payloads for the isolated integration candidate.
art/source/characters/achilles/canonical/*.blend filter=lfs diff=lfs merge=lfs -text
assets/characters/Achilles/3d/*.glb filter=lfs diff=lfs merge=lfs -text
