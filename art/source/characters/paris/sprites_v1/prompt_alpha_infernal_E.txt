Produce a TRANSPARENT PNG with actual alpha transparency. Edit the supplied orange fire demon Greek warrior sprite sheet: remove the ENTIRE grey-and-white checkerboard background to actual transparent pixels, preserving every character, flame and whip pixel exactly. Keep the existing layout, same proportions, same poses, all character artwork unchanged. No checkerboard, no white or black fill in the output: native alpha=0 everywhere between characters. Keep antialiasing alpha at character/flame edges. Output just the isolated sprite sheet, no presentation background.
