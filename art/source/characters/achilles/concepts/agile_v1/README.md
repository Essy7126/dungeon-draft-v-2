# Proposition écartée — trop détaillée

L'image `achille_concept_v1.png` a été générée avec ImageGen intégré le 10 septembre
2026, en 1024 × 1536. L'utilisateur l'a rejetée : encore trop détaillée ; demande
d'un dessin simple comparable aux personnages Dofus, avec une bonne résolution.

`generation_prompt.txt` est le prompt exécuté. `correction_prompt.txt` était une
préparation : il n'a pas été exécuté, car le retour utilisateur a réorienté
l'essai vers une création graphique neuve dans `../graphic_v1/`.
Ne pas utiliser cette proposition comme référence artistique validée.
